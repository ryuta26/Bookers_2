<div class="container">
  <div class="row">
	<div class="col-sm-3">
			<%= render 'users/info', user: @user %>
			<%= render 'books/new', book: @book %>
	</div>

	<div class="col-sm-9">
		<%= render 'books/list', book: @book %>
	</div>
  </div>
</div>