<div class="container">
    <div class="row">
        <div class="col-xs-3">
            <%= render 'users/info', user: @user %>
            <%= render 'books/new', book: @book %>
        </div>
        <div class="col-xs-9">
            <%= render 'books/list', book: @book %>
        </div>
    </div>
</div>